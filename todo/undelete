UNDELETE
        int j = long long start !
        option instead of -b a "use what's there"
        option, esp for files that are *not* deleted, i.e. recovery
        explain why defrag is king

fix -u option when it uses a parameter
atm, it needs "-uarg", "-u arg" confuses it
